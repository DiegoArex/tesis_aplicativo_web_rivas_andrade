package com.example.demo.entity;

/**
 * DEPRECATED: Esta clase ya no se usa.
 * Se prefiere CustomRevisionListener en com.example.demo.config
 * que es la que realmente captura la información del usuario.
 */
@Deprecated
public class AuditoriaRevisionListener {
    // Mantiene compatibilidad pero no se ejecuta
}
